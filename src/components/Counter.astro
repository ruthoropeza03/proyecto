---
// El código del frontmatter se ejecuta en el servidor
const initialCount = 0;
import '../styles/global.css';
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>
<body>
    <div id="counter-container">
    <p>Contador: <span id="count-value">{initialCount}</span></p>
    <button id="increment-btn" type="button" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>Incrementar</button>
    <button id="decrement-btn" type="button" class="btn btn-danger">
                <i class="bi bi-dash-circle"></i>Decrementar</button>
</div>

<script is:inline>
// Este código se ejecuta en el cliente
if (typeof window !== 'undefined') {
    document.addEventListener('DOMContentLoaded', () => {
        const countElement = document.getElementById('count-value');
        const incrementBtn = document.getElementById('increment-btn');
        const decrementBtn = document.getElementById('decrement-btn');
        
        let count = parseInt(countElement.textContent);
        
        incrementBtn.addEventListener('click', () => {
            count++;
            countElement.textContent = count;
        });
        
        decrementBtn.addEventListener('click', () => {
            count--;
            countElement.textContent = count;
        });
    });
}
</script>
</body>
</html>

<!-- <style>
#counter-container {
    margin: 1rem 0;
    padding: 1rem;
    border: 1px solid #ddd;
    border-radius: 4px;
}

button {
    margin-right: 0.5rem;
    padding: 0.5rem 1rem;
    cursor: pointer;
}
</style> -->