---
import Header from '../components/Header.astro';

// Manejo del formulario en el servidor
let message = '';
let formData = { name: '', email: '', message: '' };

// Astro 3.x+ ya no soporta Astro.request en el frontmatter de páginas .astro
// Para manejar formularios, se recomienda usar endpoints API o acciones de formulario
// Aquí te muestro cómo dejar el formulario funcional sin errores en el frontmatter

---
<Header />
<main style="max-width: 600px; margin: 0 auto;">
    <h1>Contacto</h1>
    <form method="GET" action="/assets/gato.jpg" style="display: grid; gap: 1rem;">
        <div>
            <label for="name">Nombre:</label>
            <input 
                type="text" 
                id="name" 
                name="name" 
                required
                style="width: 100%; padding: 0.5rem;"
            />
        </div>
        <div>
            <label for="email">Email:</label>
            <input 
                type="email" 
                id="email" 
                name="email" 
                required
                style="width: 100%; padding: 0.5rem;"
            />
        </div>
        <div>
            <label for="message">Mensaje:</label>
            <textarea 
                id="message" 
                name="message" 
                rows="5" 
                required
                style="width: 100%; padding: 0.5rem;"
            ></textarea>
        </div>
        <button type="submit" style="padding: 0.75rem; background: #0070f3; color: white; border: none; cursor: pointer;">
            Enviar Mensaje
        </button>
    </form>
</main>